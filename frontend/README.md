# Sentinel screening prototype

This is a static frontend prototype. Open `frontend.html` directly in a browser, or serve the folder with any static file server.

Document uploads are handled locally by the interface for demonstration. No backend or document data service is included.
